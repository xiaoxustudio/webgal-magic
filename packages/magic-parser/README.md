# webgal-magic-parser

A library for parsing and manipulating WebGal scripts.

## Development

- Install dependencies:

```bash
vp install
```

- Run the unit tests:

```bash
vp test
```

- Build the library:

```bash
vp pack
```
