{
  "name": "webgal-magic",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vp run webgal-magic-parser#test",
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r",
    "dev": "vp run playground#dev",
    "dev:parser": "vp run webgal-magic-parser#dev",
    "build": "vp run playground#build",
    "build:parser": "vp run webgal-magic-parser#build",
    "benchmark": "vp run webgal-magic-parser#benchmark",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
